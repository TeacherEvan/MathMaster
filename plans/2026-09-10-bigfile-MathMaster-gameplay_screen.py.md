# Refactor plan — `src/mathmistress/gameplay_screen.py` in `TeacherEvan/MathMaster`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-10T17:58:40.372784+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/MathMaster` |
| path | `src/mathmistress/gameplay_screen.py` |
| size | 155.2 KB (158,906 bytes) |
| lines | 3,044 |
| generated | 2026-09-10T17:58:40.372809+07:00 |

### Top imports (sample)
  - `import tkinter as tk`
  - `import random`
  - `import time`
  - `import logging`
  - `import math  # For math functions in gameplay animations`
  - `import json`
  - `import os`
  - `import traceback # Added import`
  - `from src.ui_components.feedback_manager import FeedbackManager # Added import`
  - `from lock_animation_improved import LockAnimation # Import the improved lock animation class`

## Structural analysis

**Repeated literals (≥2×):**
- `solution_symbol_display` (×5)
- `{char_to_reveal_for_log}` (×5)
- ` not in problem.split(` (×4)
- `sol_{line_idx}_{char_idx}` (×4)
- `, Extracted equation: ` (×3)
- `Click HELP button for algebra assistance` (×3)
- `level_{self.current_level}.json` (×3)
- ` prefix found but structure unclear. Using: ` (×2)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `MathMaster`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `'solution_symbol_display'` (×5) to a constant
- **Target:** String literal appears 5× in `src/mathmistress/gameplay_screen.py`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `src/mathmistress/gameplay_screen.py` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' src/mathmistress/gameplay_screen.py` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/mathmistress/gameplay_screen.py` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/mathmistress/gameplay_screen.py`.
- **Validation:** `rg -n '^import' src/mathmistress/gameplay_screen.py` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-003 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/mathmistress/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-004 — Reduce `src/mathmistress/gameplay_screen.py` below 1522 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/mathmistress/gameplay_screen.py` shrinks from 3,044 to ≤1522 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-005 — Hardening pass 5
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/mathmistress/gameplay_screen.py` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/mathmistress/gameplay_screen.py` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `MathMaster`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-10-bigfile-MathMaster-gameplay_screen.py.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-10T18:21:49.449204+07:00

**Verdict:** `NEEDS-REVISION`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. **Language mismatch — plan targets TypeScript tooling on a Python file.** The file is `gameplay_screen.py` (Python: `import tkinter`, `import random`, `import time`), but every objective assumes TypeScript: `constants.ts`, `strings.ts`, `index.ts` barrels, `pnpm dlx knip`, `ts-prune`, `export` statements, `type-check`. None of these tools or file conventions apply to a 3,044-line Python module. The structural analysis (repeated literals, imports) is valid for Python, but the extraction plan is entirely wrong-language.
2. **OBJ-005 through OBJ-012 are identical filler.** Eight of twelve objectives are verbatim copies of "Hardening pass N — Tighten types / tighten prop drilling… no `any` added." This is the generic N-slice filler the plan header explicitly claims it avoids. They add zero value and were not derived from any structural signal.
3. **OBJ-002 and OBJ-003 are inapplicable.** Running `knip`/`ts-prune` against a `.py` file and auditing `index.ts` barrels for a Python module are nonsensical. These objectives should be removed or replaced with Python-appropriate dead-code detection (e.g., `pyflakes`, `vulture`) and Python barrel/`__init__.py` audits if applicable.
